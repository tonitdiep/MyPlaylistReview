<!DOCTYPE html>
<html>
  <head>
    <title>MyPlaylistReview</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
<br><br>
        <%= link_to "Google Log In Here", '/auth/google_oauth2', method: :post%>
      
      <div>
        <% if logged_in? %>
          <%= link_to "Create Playlist", new_playlist_path %>  |
          <%= link_to "View Your Playlists", playlists_path %>  |
          <%= link_to "Add Your Review", new_review_path %>  |
          <%= link_to "Log Out", logout_path, method: "delete"%>

        <% else %>
          <%= link_to "Sign Up", '/signup'%>  |
          <%= link_to "Log In", '/login'%>
        <% end %>
      </div>
      <%= @error %>

    <%= yield %>
  </body>
</html>
